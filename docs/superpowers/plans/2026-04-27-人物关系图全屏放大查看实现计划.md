# 人物关系图全屏放大查看 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为当前人物关系图增加“放大查看”全屏弹窗，并在放大态中提供顶部轻信息栏与右下角轻详情卡，同时复用主图的筛选、选中与高亮状态。

**Architecture:** 在现有 `GraphWorkspace` 上增加一个受控的全屏查看态，不新开页面，不复制图谱状态。继续复用 `GraphCanvas` 作为渲染核心，通过新增 `GraphFullscreenDialog` 和 `GraphCanvasInfoBar` 组织全屏 UI，并让主图与放大态共享同一套 `scope / selectedNodeId / detail`。

**Tech Stack:** Next.js 14、React 18、TypeScript、Cytoscape.js、Vitest

---

## File Structure

### Existing files to modify

- Modify: `src/components/graph-workspace.tsx`
- Modify: `src/components/graph-canvas.tsx`
- Modify: `src/app/globals.css`
- Modify: `tests/graph-workspace.test.tsx`
- Modify: `tests/graph-page.test.tsx`

### New files to create

- Create: `src/components/graph-fullscreen-dialog.tsx`
- Create: `src/components/graph-canvas-info-bar.tsx`
- Create: `tests/graph-fullscreen-dialog.test.tsx`

### Responsibility map

- `src/components/graph-workspace.tsx`: 新增放大态开关，连接主图与全屏图的共享状态
- `src/components/graph-fullscreen-dialog.tsx`: 管理全屏遮罩、关闭交互、放大态布局
- `src/components/graph-canvas-info-bar.tsx`: 顶部轻信息栏
- `src/components/graph-canvas.tsx`: 继续复用同一套 Cytoscape 画布能力，让放大态沿用点击、缩放、平移、不可拖拽
- `src/app/globals.css`: 增加全屏弹窗、轻信息栏、右下角详情卡样式
- `tests/*.test.tsx`: 用 SSR 壳测试锁住入口、信息栏、轻详情卡与关闭按钮文案

## Task 1: Add Fullscreen UI Shell Components

**Files:**
- Create: `src/components/graph-canvas-info-bar.tsx`
- Create: `src/components/graph-fullscreen-dialog.tsx`
- Test: `tests/graph-fullscreen-dialog.test.tsx`

- [ ] **Step 1: Write the failing fullscreen shell tests**

Create `tests/graph-fullscreen-dialog.test.tsx`:

```tsx
import React from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { GraphFullscreenDialog } from "@/components/graph-fullscreen-dialog";
import type { GraphDetailState } from "@/lib/graph-view";

globalThis.React = React;

const overviewDetail: GraphDetailState = {
  kind: "overview",
  counts: {
    nodes: 4,
    edges: 3
  },
  node: undefined,
  relations: []
};

describe("GraphFullscreenDialog", () => {
  it("renders the info bar and close control when open", () => {
    const markup = renderToStaticMarkup(
      <GraphFullscreenDialog
        isOpen
        title="玻璃城遗闻"
        scopeLabel="人物 + 势力"
        selectedNodeName={undefined}
        detail={overviewDetail}
        onClose={() => undefined}
      >
        <div>关系图画布</div>
      </GraphFullscreenDialog>
    );

    expect(markup).toContain("滚轮缩放，拖动画布移动，点击节点查看关系");
    expect(markup).toContain("人物 + 势力");
    expect(markup).toContain("关闭");
  });

  it("does not render anything when closed", () => {
    const markup = renderToStaticMarkup(
      <GraphFullscreenDialog
        isOpen={false}
        title="玻璃城遗闻"
        scopeLabel="人物"
        selectedNodeName={undefined}
        detail={overviewDetail}
        onClose={() => undefined}
      >
        <div>关系图画布</div>
      </GraphFullscreenDialog>
    );

    expect(markup).toBe("");
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run:

```bash
npm run test -- tests/graph-fullscreen-dialog.test.tsx
```

Expected: FAIL because `GraphFullscreenDialog` does not exist yet.

- [ ] **Step 3: Implement the minimal fullscreen shell**

Create `src/components/graph-canvas-info-bar.tsx`:

```tsx
interface GraphCanvasInfoBarProps {
  title: string;
  scopeLabel: string;
  selectedNodeName?: string;
  onClose?: () => void;
}

export function GraphCanvasInfoBar({
  title,
  scopeLabel,
  selectedNodeName,
  onClose
}: GraphCanvasInfoBarProps) {
  return (
    <div className="graph-fullscreen-info-bar">
      <div className="graph-fullscreen-info-copy">
        <strong>{title}</strong>
        <span>{scopeLabel}</span>
        {selectedNodeName ? <span>当前选中：{selectedNodeName}</span> : null}
        <span>滚轮缩放，拖动画布移动，点击节点查看关系</span>
      </div>
      {onClose ? (
        <button type="button" className="button-secondary" onClick={onClose}>
          关闭
        </button>
      ) : null}
    </div>
  );
}
```

Create `src/components/graph-fullscreen-dialog.tsx`:

```tsx
import type { ReactNode } from "react";
import type { GraphDetailState } from "@/lib/graph-view";
import { GraphCanvasInfoBar } from "@/components/graph-canvas-info-bar";

interface GraphFullscreenDialogProps {
  isOpen: boolean;
  title: string;
  scopeLabel: string;
  selectedNodeName?: string;
  detail: GraphDetailState;
  onClose: () => void;
  children: ReactNode;
}

export function GraphFullscreenDialog({
  isOpen,
  title,
  scopeLabel,
  selectedNodeName,
  detail,
  onClose,
  children
}: GraphFullscreenDialogProps) {
  if (!isOpen) {
    return null;
  }

  return (
    <div className="graph-fullscreen-overlay" role="dialog" aria-modal="true" aria-label="放大查看关系图">
      <div className="graph-fullscreen-backdrop" onClick={onClose} />
      <div className="graph-fullscreen-shell">
        <GraphCanvasInfoBar
          title={title}
          scopeLabel={scopeLabel}
          selectedNodeName={selectedNodeName}
          onClose={onClose}
        />
        <div className="graph-fullscreen-stage">{children}</div>
        {detail.kind === "node" ? (
          <div className="graph-fullscreen-detail-card">
            <strong>{detail.node.name}</strong>
            <span>{detail.node.type}</span>
            {detail.node.summary ? <p>{detail.node.summary}</p> : null}
            <ul className="plain-list compact-list">
              {detail.relations.slice(0, 3).map((item) => (
                <li key={item.edge.id}>
                  {item.target.name}
                  {item.edge.description ? ` · ${item.edge.description}` : ""}
                </li>
              ))}
            </ul>
          </div>
        ) : null}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Run the shell test to verify it passes**

Run:

```bash
npm run test -- tests/graph-fullscreen-dialog.test.tsx
```

Expected: PASS

- [ ] **Step 5: Checkpoint the fullscreen shell**

Current workspace is not a git repo, so use a simple file checkpoint:

```bash
printf '%s\n' src/components/graph-canvas-info-bar.tsx src/components/graph-fullscreen-dialog.tsx tests/graph-fullscreen-dialog.test.tsx
```

Expected: the three file paths print.

If git is later initialized, use:

```bash
git add src/components/graph-canvas-info-bar.tsx src/components/graph-fullscreen-dialog.tsx tests/graph-fullscreen-dialog.test.tsx
git commit -m "feat: add graph fullscreen dialog shell"
```

## Task 2: Integrate Fullscreen State into GraphWorkspace

**Files:**
- Modify: `src/components/graph-workspace.tsx`
- Modify: `tests/graph-workspace.test.tsx`

- [ ] **Step 1: Write the failing workspace test for the fullscreen entry**

Add to `tests/graph-workspace.test.tsx`:

```tsx
it("renders the fullscreen viewing entry", () => {
  const markup = renderToStaticMarkup(<GraphWorkspace graph={graph} />);

  expect(markup).toContain("放大查看");
});
```

- [ ] **Step 2: Run the workspace test to verify it fails**

Run:

```bash
npm run test -- tests/graph-workspace.test.tsx
```

Expected: FAIL because `放大查看` is not rendered yet.

- [ ] **Step 3: Implement the fullscreen state and entry button**

Update `src/components/graph-workspace.tsx`:

```tsx
import { GraphFullscreenDialog } from "@/components/graph-fullscreen-dialog";

const scopeLabels = {
  characters: "人物",
  "characters-factions": "人物 + 势力",
  all: "人物 + 势力 + 地点"
} as const;

export function GraphWorkspace({ graph }: GraphWorkspaceProps) {
  const [scope, setScope] = useState(() => getDefaultGraphScope(graph));
  const [selectedNodeId, setSelectedNodeId] = useState<string | undefined>(undefined);
  const [isFullscreenOpen, setIsFullscreenOpen] = useState(false);
  const coreRef = useRef<Core | null>(null);

  const selectedNodeName = detail.kind === "node" ? detail.node.name : undefined;

  return (
    <div className="graph-shell">
      {/* existing filter panel */}

      <section className="graph-canvas panel">
        <div className="panel-header">
          <div>
            <p className="panel-eyebrow">关系画布</p>
            <h2>{graph.novel.title}</h2>
          </div>
          <button
            type="button"
            className="button-secondary"
            onClick={() => setIsFullscreenOpen(true)}
          >
            放大查看
          </button>
        </div>

        <GraphCanvas
          graph={filteredGraph}
          selectedNodeId={selectedNodeId}
          onSelectNode={setSelectedNodeId}
          onReady={(core) => {
            coreRef.current = core;
          }}
        />
      </section>

      <GraphDetailPanel detail={detail} />

      <GraphFullscreenDialog
        isOpen={isFullscreenOpen}
        title={graph.novel.title}
        scopeLabel={scopeLabels[scope]}
        selectedNodeName={selectedNodeName}
        detail={detail}
        onClose={() => setIsFullscreenOpen(false)}
      >
        <GraphCanvas
          graph={filteredGraph}
          selectedNodeId={selectedNodeId}
          onSelectNode={setSelectedNodeId}
        />
      </GraphFullscreenDialog>
    </div>
  );
}
```

- [ ] **Step 4: Run the updated workspace tests**

Run:

```bash
npm run test -- tests/graph-workspace.test.tsx tests/graph-fullscreen-dialog.test.tsx
```

Expected: PASS

- [ ] **Step 5: Checkpoint the shared-state integration**

```bash
printf '%s\n' src/components/graph-workspace.tsx tests/graph-workspace.test.tsx
```

Expected: both file paths print.

If git is later initialized, use:

```bash
git add src/components/graph-workspace.tsx tests/graph-workspace.test.tsx
git commit -m "feat: add fullscreen graph viewer entry"
```

## Task 3: Add Close Behavior and Fullscreen Styling

**Files:**
- Modify: `src/components/graph-fullscreen-dialog.tsx`
- Modify: `src/app/globals.css`
- Test: `tests/graph-fullscreen-dialog.test.tsx`

- [ ] **Step 1: Extend the failing test to cover the lightweight detail card**

Add to `tests/graph-fullscreen-dialog.test.tsx`:

```tsx
const nodeDetail: GraphDetailState = {
  kind: "node",
  counts: {
    nodes: 4,
    edges: 3
  },
  node: {
    id: "c1",
    name: "祝衡",
    type: "CHARACTER",
    summary: "档案修复师",
    tags: []
  },
  relations: [
    {
      edge: {
        id: "e1",
        sourceId: "c1",
        targetId: "f1",
        type: "OTHER",
        description: "隶属关系"
      },
      target: {
        id: "f1",
        name: "城档馆",
        type: "FACTION",
        summary: "档案机构",
        tags: []
      }
    }
  ]
};

it("renders the lightweight detail card when a node is selected", () => {
  const markup = renderToStaticMarkup(
    <GraphFullscreenDialog
      isOpen
      title="玻璃城遗闻"
      scopeLabel="人物"
      selectedNodeName="祝衡"
      detail={nodeDetail}
      onClose={() => undefined}
    >
      <div>关系图画布</div>
    </GraphFullscreenDialog>
  );

  expect(markup).toContain("祝衡");
  expect(markup).toContain("档案修复师");
  expect(markup).toContain("城档馆");
});
```

- [ ] **Step 2: Run the test to verify it fails if the card is missing**

Run:

```bash
npm run test -- tests/graph-fullscreen-dialog.test.tsx
```

Expected: FAIL if the lightweight detail card has not been rendered yet.

- [ ] **Step 3: Implement the final dialog behavior and styles**

Update `src/components/graph-fullscreen-dialog.tsx` to support `Escape`:

```tsx
import { useEffect } from "react";

useEffect(() => {
  if (!isOpen) {
    return;
  }

  function handleKeyDown(event: KeyboardEvent) {
    if (event.key === "Escape") {
      onClose();
    }
  }

  window.addEventListener("keydown", handleKeyDown);
  return () => {
    window.removeEventListener("keydown", handleKeyDown);
  };
}, [isOpen, onClose]);
```

Add CSS in `src/app/globals.css`:

```css
.graph-fullscreen-overlay {
  position: fixed;
  inset: 0;
  z-index: 50;
}

.graph-fullscreen-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(24, 18, 14, 0.72);
}

.graph-fullscreen-shell {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-rows: auto 1fr;
  gap: 16px;
  width: min(1440px, calc(100vw - 32px));
  height: min(92vh, calc(100vh - 32px));
  margin: 16px auto;
  padding: 18px;
  border-radius: 24px;
  background: rgba(247, 241, 232, 0.96);
  box-shadow: 0 24px 80px rgba(31, 24, 18, 0.32);
}

.graph-fullscreen-info-bar {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: flex-start;
}

.graph-fullscreen-info-copy {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  color: rgba(59, 43, 27, 0.82);
}

.graph-fullscreen-stage {
  position: relative;
  min-height: 0;
}

.graph-fullscreen-stage .graph-canvas-stage {
  min-height: 100%;
  height: 100%;
}

.graph-fullscreen-detail-card {
  position: absolute;
  right: 18px;
  bottom: 18px;
  width: min(320px, calc(100vw - 80px));
  display: grid;
  gap: 10px;
  padding: 14px 16px;
  border-radius: 18px;
  background: rgba(255, 252, 246, 0.94);
  border: 1px solid rgba(89, 67, 37, 0.12);
  box-shadow: 0 12px 30px rgba(31, 24, 18, 0.16);
}
```

- [ ] **Step 4: Run the dialog-focused tests**

Run:

```bash
npm run test -- tests/graph-fullscreen-dialog.test.tsx tests/graph-workspace.test.tsx tests/graph-page.test.tsx
```

Expected: PASS

- [ ] **Step 5: Checkpoint fullscreen polish**

```bash
printf '%s\n' src/components/graph-fullscreen-dialog.tsx src/app/globals.css tests/graph-fullscreen-dialog.test.tsx
```

Expected: the three file paths print.

If git is later initialized, use:

```bash
git add src/components/graph-fullscreen-dialog.tsx src/app/globals.css tests/graph-fullscreen-dialog.test.tsx
git commit -m "feat: polish fullscreen graph viewer interactions"
```

## Task 4: Final Verification

**Files:**
- Test: `tests/graph-fullscreen-dialog.test.tsx`
- Test: `tests/graph-workspace.test.tsx`
- Test: `tests/graph-page.test.tsx`
- Test: `tests/graph-canvas.test.ts`

- [ ] **Step 1: Run the targeted fullscreen regression suite**

Run:

```bash
npm run test -- tests/graph-fullscreen-dialog.test.tsx tests/graph-workspace.test.tsx tests/graph-page.test.tsx tests/graph-canvas.test.ts
```

Expected: all fullscreen-related tests PASS.

- [ ] **Step 2: Run build to verify client-only dialog code does not break production**

Run:

```bash
npm run build
```

Expected: PASS. Build may still log the known Supabase pooler reachability warnings during static generation fallback, but the build itself must succeed.

- [ ] **Step 3: Run typecheck after build finishes**

Run:

```bash
npm run typecheck
```

Expected: PASS. Run this after `build`, not in parallel, because `.next/types` is regenerated during build.

- [ ] **Step 4: Final checkpoint**

```bash
printf '%s\n' src/components/graph-canvas-info-bar.tsx src/components/graph-fullscreen-dialog.tsx src/components/graph-workspace.tsx src/app/globals.css tests/graph-fullscreen-dialog.test.tsx tests/graph-workspace.test.tsx tests/graph-page.test.tsx
```

Expected: all listed file paths print.

If git is later initialized, use:

```bash
git add src/components/graph-canvas-info-bar.tsx src/components/graph-fullscreen-dialog.tsx src/components/graph-workspace.tsx src/app/globals.css tests/graph-fullscreen-dialog.test.tsx tests/graph-workspace.test.tsx tests/graph-page.test.tsx
git commit -m "feat: add fullscreen graph viewing mode"
```

## Self-Review

### Spec coverage

- `放大查看入口`: Task 2
- `全屏弹窗`: Tasks 1 and 3
- `顶部轻信息栏`: Tasks 1 and 3
- `右下角轻详情卡`: Task 3
- `共享选中 / 筛选状态`: Task 2
- `Esc / 关闭按钮 / 遮罩关闭`: Task 3
- `保留缩放 / 平移 / 点击查看，不恢复拖拽`: Tasks 2, 3, and final regression in Task 4

### Placeholder scan

- No `TODO` / `TBD`
- Every task has explicit test and verification commands
- All code steps specify concrete file paths and component names

### Type consistency

- `GraphFullscreenDialog` consistently receives `isOpen / title / scopeLabel / selectedNodeName / detail / onClose`
- `GraphCanvasInfoBar` consistently owns the lightweight top summary
- `GraphWorkspace` remains the single source of truth for `scope` and `selectedNodeId`

Plan complete and saved to `docs/superpowers/plans/2026-04-27-人物关系图全屏放大查看实现计划.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
